"use client"

import { useState } from "react"

export const HomePage = () => {
  // uncomment below makes it work
  // const [count, setCount] = useState(0)
  return <div>Client-side rendered home page</div>
}
